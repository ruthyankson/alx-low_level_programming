#include "main.h"
/**
 *mul - multiplies two integers.
 *@a: first integer
 *@b: second integer
 *Return: result after multiplication
 */
int mul(int a, int b)
{
	int result = a * b;
	
	return (result);
}
