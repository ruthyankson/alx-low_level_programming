#include "main.h"

/**
 *_abs - computes the absolute value of an integer.
 *@int: input value (int)
 *Return: the absolute value of the input number (Success)
 */

int _abs(int)
{
	return (int * ((v > 0) - (v < 0));
}
